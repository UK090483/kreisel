// import { useEffect } from "react";
// import scrollama from "scrollama";

// const scroll = scrollama();

// const fakeCallback = () => null;

// const useInView = (
//   options: scrollama.ScrollamaOptions,
//   callbacks: {
//     onStepEnter?: scrollama.StepCallback;
//     onStepExit?: scrollama.StepCallback;
//     onStepProgress?: scrollama.StepProgressCallback;
//   }
// ) => {
//   useEffect(() => {
//     scroll
//       .setup(options)
//       .onStepEnter(callbacks.onStepEnter || fakeCallback)
//       .onStepExit(callbacks.onStepExit || fakeCallback)
//       .onStepProgress(callbacks.onStepProgress || fakeCallback);
//   }, [options, callbacks]);
// };
// export default useInView;
