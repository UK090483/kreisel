const domain = "developermail.com";
const firstName = "test__kreisel__user";
const name = "testuser_lastename";
const oneTimePassword = "oneTimePassword";

const testUser = {
  mail: `${name}@${domain}`,
  name,
  firstName,
  oneTimePassword,
};

export default testUser;
